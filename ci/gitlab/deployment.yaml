trigger-server:
  needs:
    - job: build-docker-tag
  variables:
    TAG: $CI_COMMIT_TAG
  stage: deploy
  trigger: server-management/main-server.ulbricht.casa/bambushain
  rules:
    - if: $CI_COMMIT_TAG