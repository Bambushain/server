apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: {{ include "bambushain.namespace" . }}
  labels:
    {{- include "bambushain.labels" . | nindent 4 }}
spec:
  selector:
    bambushain.app/app: postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432